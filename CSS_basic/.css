.red{
    width: 100px;
    height: 100px;
    /* padding : content 안에 들어가는 공간을 의미함 */
    /* top, bottom, left, right */
    padding:20px,20px;
    /* margin : content 밖에 들어가는 공간을 의미함 */
    margin: 20px;
    /* border : content 테두리 */
    /* width, style, color*/
    border: 2px dashed red;
    background: yellow;
}